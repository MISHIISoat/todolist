spring:
  datasource:
    url: jdbc:postgresql://localhost:2345/todolist
    username: todolist-postgres
    password: todolist-password
  jpa:
    hibernate.ddl-auto: update
    properties.hibernate.dialect: org.hibernate.dialect.PostgreSQLDialect